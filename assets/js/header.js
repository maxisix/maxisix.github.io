define(['jquery'], function($){
		

	// $(".header").on('mousemove', function() {
	//   var that = $(this);

	//   that.addClass('is-animated');
	// });

	// $(".header").on('mouseleave', function() {
	//   var that = $(this);

	//   that.removeClass('is-animated');
	// });


	// var images = ['pattern1', 'pattern2'];
	// var header = $('.header');
	// header.css({'background-image': 'url(assets/images/' + images[Math.floor(Math.random() * images.length)] + '.jpg)'});





});